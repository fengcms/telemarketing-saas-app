# 2026-07-23 开发日志

## 环境搭建（新电脑）

### 已安装
- Flutter SDK（通过 Homebrew）
- 编译 APK 成功
- 真机 Redmi K60 USB 部署成功

### 踩坑
- `tdesign_flutter 0.2.7` 与 Dart 3.12 兼容问题：`_TDIconsData extends IconData`（IconData 变为 final class）
- 修复方法：Python 脚本批量替换 2115 个图标常量为直接 `IconData(...)` 构造

## Node 1 — 线索详情页数据层

### 新增文件
- `lib/models/lead_detail.dart` — 线索详情模型（含 project/owner 内嵌对象）
- `lib/models/follow_up_record.dart` — 跟进记录模型
- `lib/models/call_record.dart` — 通话记录模型
- `lib/models/lead_list_context.dart` — 列表上下文模型（底部导航辅助）
- `lib/providers/lead_detail_provider.dart` — 线索详情状态管理器（3 个并行请求、底部导航切换、局部刷新）

### 修改文件
- `lib/services/api_constants.dart` — 新增 `calls` 端点
- `lib/services/lead_service.dart` — 新增 8 个方法（详情/跟进/通话/编辑/创建/补正/日程）
- `lib/constants/lead_constants.dart` — 新增 `answerTypeLabels` 映射

## Node 2 — 页面骨架 + 头部 + 操作按钮

### 新增文件
- `lib/pages/leads/lead_detail_page.dart` — 主页面（CustomScrollView + SliverAppBar 可折叠 + 骨架屏 + 错误态）
- `lib/pages/leads/widgets/lead_header_section.dart` — Section A（姓名+标签+电话号码+详细信息）
- `lib/pages/leads/widgets/lead_action_bar.dart` — Section B（拨号/跟进/预约/编辑 4 按钮）

### 修改文件
- `lib/pages/leads/leads_list_page.dart` — 卡片点击跳转详情页，携带 listContext

### 关键决策
- TDNavBar 在 0.2.7 版本中接受 `leftBarItems`/`rightBarItems`（TDNavBarItem 列表），不是 `leading`
- TDButton `type: TDButtonType.text` 实现纯文字按钮（不是 `theme: TDButtonTheme.text`）
- 电话拨号 FAB 使用 Material FloatingActionButton（TDFloatingActionButton 在 0.2.7 中不存在）
- 导航使用 Navigator.push（go_router 虽在 pubspec 中但未启用）

## Node 3 — 跟进时间线（Section C）

### 新增文件
- `lib/pages/leads/widgets/follow_up_card.dart` — 单条跟进记录卡片（时间/跟进人/接听类型标签+时长/内容/分类变更/编辑删除按钮）
- `lib/pages/leads/widgets/follow_up_timeline.dart` — 跟进时间线组件（圆点+连线布局/加载更多本地切片/空态/骨架屏）

### 修改文件
- `lib/pages/leads/lead_detail_page.dart` — 接入 FollowUpTimeline

### 关键决策
- 编辑按钮可见性前端计算：TE = 本人+≤5分钟，TM/TA = 全部
- 删除按钮：TE = 本人（不限时间），TM/TA = 全部
- 时间线圆点：已接听/最新=brand-7实心，未接听=gray-4空心
- 本地切片渲染（API一次返回全量，前端分 _batchSize=10 条展示）
- 跟进人姓名暂时显示 userId（后接 OptionsCacheService 解析）
- 分类变更通过与上一条记录的 categoryId 比对实现
- 编辑/删除按钮 onTap 留桩待 Node 5

## Node 4 — 通话记录摘要 + 底部导航条

### 新增文件
- `lib/pages/leads/widgets/call_records_section.dart` — Section D（标题+查看全部、3条通话记录行、接听类型标签、补正按钮TM/TA、空态）
- `lib/pages/leads/widgets/lead_bottom_nav.dart` — 底部线索导航条（上一个/计数/下一个、禁用态gray-4、仅listContext存在时显示）

### 修改文件
- `lib/pages/leads/lead_detail_page.dart` — 接入 CallRecordsSection + LeadBottomNav（bottomNavigationBar）

### 当前状态
- Node 1-4 全部完成，0 个编译错误

## Node 5a — 跟进面板 + 编辑跟进弹窗

### 新增文件
- `lib/pages/leads/widgets/follow_up_panel.dart` — 跟进面板（showModalBottomSheet）：TDTextarea 内容+字数计数、接听类型5选1（Material 自定义）、通话时长 TDStepper×2、分类选择、提交校验+API+TDToast
- `lib/pages/leads/widgets/edit_follow_up_dialog.dart` — 编辑跟进弹窗（AlertDialog）：预填内容、TDTextarea、保存API+刷新

### 修改文件
- `lib/pages/leads/widgets/lead_action_bar.dart` — "跟进"按钮连接 FollowUpPanel（新增 leadId 参数）
- `lib/pages/leads/widgets/lead_action_bar.dart` — "跟进"按钮连接 FollowUpPanel
- `lib/pages/leads/widgets/follow_up_timeline.dart` — "编辑"按钮连接 EditFollowUpDialog（新增 leadId 参数）
- `lib/pages/leads/lead_detail_page.dart` — 传递 leadId 给子组件

### 踩坑
- TDCheckTag 在 tdesign_flutter 0.2.7 中不存在，改用 Material Container 自定义选择器
- TDPopup/TDDialog 无 show 方法，改用 Flutter 原生 showModalBottomSheet/showDialog

## Node 5b — 预约弹窗 + 编辑线索 + 拨号流程

### 新增文件
- `lib/pages/leads/widgets/schedule_dialog.dart` — 预约弹窗（日期选择器TDPicker.showDatePicker、时间选择showTimePicker、备注、过去时间校验+API+TDToast）
- `lib/pages/leads/widgets/edit_lead_dialog.dart` — 编辑线索弹窗（分类从OptionsCacheService加载、状态前向流转TE限制、底部Sheet选择器、API+刷新+TDToast）
- `lib/pages/leads/widgets/dial_helper.dart` — 拨号辅助（夜间禁呼时段检查、确认弹窗、url_launcher tel:）

### 修改文件
- `lib/pages/leads/widgets/lead_action_bar.dart` — 拨号/预约/编辑按钮全部连接弹窗
- `lib/pages/leads/widgets/lead_header_section.dart` — 电话FAB连接拨号流程
- `pubspec.yaml` — 添加 url_launcher 依赖

### 修 bug
- `flutter pub get` 使用系统代理连接 pub.flutter-io.cn 超时；需 `no_proxy=* flutter pub get` 绕过代理

## Node 5c — 通话补正 + 删除确认 + 夜间禁呼

### 新增文件
- `lib/pages/leads/widgets/delete_confirm_dialog.dart` — 删除确认弹窗
- `lib/pages/leads/widgets/correct_call_dialog.dart` — 通话补正弹窗（接听类型+时长+预填）

### 修改文件
- `lib/pages/leads/widgets/follow_up_timeline.dart` — 删除按钮连接删除确认弹窗
- `lib/pages/leads/widgets/call_records_section.dart` — 补正按钮连接通话补正弹窗

### 最终状态
- 线索详情页（05）全部 5 个节点开发完成
- 全项目 0 个编译错误
- MILESTONES.md 待更新
- 还剩 Node 5（全部弹窗/面板交互）待开发

## 最终交接
- 编写 `docs/dev/HANDOVER_05_LEAD_DETAIL.md` 列出未完成项
- P0（功能缺失 5 项）：通话反馈面板、「查看全部」跳转、跟进面板分类选择器、夜间禁呼配置、onResume 反馈
- P1（UI/UX 6 项）：圆点连线高度固定、结束时间缺失等
- P2（优化 5 项）：预加载策略、骨架动画等
- git commit & push

## 本地打包安装验证（本会话接续 12:2x）
- 同步 master 最新提交 feat: 线索详情页完整开发（82e6ec9）
- flutter build apk --debug 成功（27s），产物 build/app/outputs/flutter-apk/app-debug.apk
- adb install -r 到 Redmi K60（3e06fd6d / Android 16）成功
- 启动衔接验证：Dart VM service listening 成功、Impeller+Vulkan 渲染后端就绪、UI locale 进入 zh-CN、ProfileInstaller 完成、全程无 FATAL/崩溃/Flutter 错误
- 踩坑记录：①flutter install 在本机会误找 app-release.apk（不存在）导致安装失败，改用 adb install -r 直接装 debug apk 成功；②本机 adb 不在 PATH（which adb 为空），实际路径 ~/Library/Android/sdk/platform-tools/adb（flutter devices 能识别设备但 which 找不到）；③flutter run --use-application-binary 配合 `| tail` 会缓冲日志看不到实时输出，改用 adb logcat --pid 直抓
- MIUI 限制：uiautomator dump 无输出，无法自动模拟点击；列表→详情→拨号的深度交互衔接需真机手动验证
- 已知待补 P0（来自 HANDOVER）：拨号后通话反馈面板(07)、onResume 自动弹面板、查看全部跳转、跟进面板分类选择器、夜间禁呼配置读取

## 跟进面板四項修改（13:2x 真机验证后调整）
- 1. textarea: minLines=2, maxLength=100（原 2000），高度显著降低
- 2. 接听类型: 一行 5 个平铺（原 4 个）
- 3. 分类选择器: 改 label "线索分类"（原"修改分类（可选）"）、去掉"不修改"chip、加载时自动选中分类列表第一项 → 后改为选中线索自身 categoryId
- 4. 通话时长: 删除手动 TDStepper 输入 → 通过 Android MethodChannel（`com.example.telemarketing_app/call_log`）+ `READ_CALL_LOG` 权限，查询系统通话记录最近 5 分钟匹配线索手机号的通话时长(秒)
- 涉及文件: `follow_up_panel.dart`、`MainActivity.kt`、`AndroidManifest.xml`
- 编译零报错 + 真机启动无崩溃

### 后续多轮迭代（13:35~14:05）
- Textarea 外填充去除（`margin: EdgeInsets.zero`）+ 内填充（`inputDecoration.contentPadding`）
- 通话时长格式：`0秒`（非`0`），`x分x秒`/`x秒`，统一 `_formatDuration()`
- 提交按钮：`iconWidget` 白色转圈 + 清空文字（与登录页一致）
- 通话记录查询插曲：`ContentResolver.query` 的 sortOrder 不支持 `LIMIT` 关键词
- 多次打包缓存问题（`flutter clean` + uninstall 彻底解决）
- 用户强调：不确定时先问，不要猜测

## 代码审阅整改（14:10~14:25）
- 审查文档来源：`docs/review/` 全部 13 份审阅
- Ｐ０（d306f28）：清理 12 个 lint — 删未用字段/导入/变量、删冗余 cast、删空 override、删 debugPrint
- Ｐ１：api_client 强转判空保护、dial_helper debugPrint 移除、首页 5 lint 清理
- flutter analyze：21 issues → 6 issues（全部 info 级 prefer_initializing_formals）

## 最终提交（审阅整改）
- `fix(follow-up-panel): 跟进面板多项 UI 调整 + 通话记录查询`
- 同步 review 文档 + 更新 HANDOVER_05_LEAD_DETAIL.md（标记 P0#4/P1#12 已解决）
- git push 成功
- 机制：`git stash` 排除未提交改动后跑干净基线 `flutter analyze` → 21 issues / 0 error（12 warning + 9 info）
- 客观手段：analyze 归因（git blame 到提交）+ 注释密度扫描 + 结构扫描（上帝方法/巨型文件）+ 调试输出/TODO 扫描 + 核心文件人工抽读
- 关键发现：
  - `d306f28` 误标 `docs:`，实为 +2230 行线索列表页功能（需返工：12 lint + 上帝方法 + 近乎无注释）
  - 上帝方法：lead_card build 144 / correct_call_dialog 141 / lead_detail_page 140 / follow_up_card 128 / leads_list `_showFilterSheet` 137
  - 巨型文件：home_page 927 / leads_list 807 / force_change 624 / follow_up_panel 563
  - 全仓通病：文件头 `///` 在 import 之后（违反 STYLE_GUIDE §2.2）；`_build*` 辅助方法普遍缺 `///`
  - `dial_helper.dart:104` debugPrint 违反 §8.2；2 处 TODO 残留
- ⚠️ 工作树有未提交 WIP（`follow_up_panel.dart` 误删 `_durationMinutes/_durationSeconds` 字段）致当前 `flutter analyze` 2 个 error，非已合入提交问题
- 产出：`docs/review/` 下 13 份提交审查文档 + `README.md` 总览索引
- 后续：一次提交一次审核，每个新提交产出独立文档

## 阶段性复检（整改后）— 14:56
- 团队据 `docs/review/RESPONSE-2026-07-23.md` 整改，新增 5 提交：e7ee7c3 / 6280df9 / 771ba90 / 3e8ce01 / d138ead（HEAD）
- 复检方法：工作树 clean 后 `flutter analyze` 实跑（0 issues 可复现）+ git blame + 结构/注释脚本重扫 + grep 核对
- 结论：A- 评级，整改通过。21 issues→0；5 处上帝方法全拆分（最大方法 144→105 行）；文件头 `///` 位置全库修复；dial_helper debugPrint 移除、2 TODO 清零、api_client 判空、lead_constants 合并均验证成立
- 新发现：① `.gitignore` 漏忽略 `.workbuddy/`，VERIFY/memory 内部文件被提交进仓库（含我之前操作痕迹，认领）；② `prefer_initializing_formals` 全局关、`use_build_context_synchronously` 单文件 ignore_for_file（建议改为局部 ignore+注释）；③ e7ee7c3/d138ead 提交原子性偏弱
- 开放项：4 巨型文件仅方法级拆分未文件级；`_build*` 注释未全补；常量命名待定稿
- 产出：`docs/review/PHASE-REVIEW-2026-07-23.md`

## 组长复核未执行项（15:20）
- 用户就 RESPONSE 中"两个巨型文件延后拆分"征求客观评价
- 通读 `force_change_password_page.dart`(633) 与 `follow_up_panel.dart`(574) 逐方法核对耦合
- 结论：延后决定对单人私有 MVP 可接受（均不违反硬规则：最大方法 78/79 行、analyze 0）；但"成本高收益低"表述不严谨——两文件各有 2–4 个零状态纯展示块可零成本抽离（被遗漏）
- 廉价抽离点：force_change 的 `_buildSecurityHint/_buildPasswordRule/_buildNavBar/_buildStrengthIndicator`；follow_up_panel 的 `_buildHeader` + `_buildCallLogContent`(收敛为数据记录)
- 真耦合簇（同意保留）：force_change 两密码输入行；follow_up_panel 的 `_checkCallLog`+3状态+接听类型选择器(需上提 CallLogController/provider)
- 已追加第八节"组长对未执行项的复核意见"到 PHASE-REVIEW 文档；定性为 P2 合理延后，要求修正笼统表述，给通用抽离模式+重审触发条件

## 预约面板改造（15:40）
- schedule_dialog.dart: AlertDialog → showModalBottomSheet 底部抽屉（跟进面板同款样式）
- 备注文本框: 去掉 SizedBox(height:80) → minLines:2 自适应 + 圆角灰边框 + 内填充
- 时间选择器: Material showTimePicker → TDesign showDatePicker(useHour+useMinute)，天然中文
- flutter analyze 零报错，真机已安装无崩溃

## 第一轮归档 + 第二轮 Sprint 审阅（16:19）
- 第一轮验收通过：将 docs/review/ 下 17 个第一轮文件（13提交审查+README+PHASE-REVIEW+RESPONSE+fix-follow-up-panel-ui）归档至 docs/review/history/，并在 history/README.md 顶部加"✅ 第一轮·已验收通过"标记
- 新写 docs/review/README.md 作为当前索引（指向 history + 新 Sprint）
- 第二轮 Sprint 审阅（输入 RESPONSE-SPRINT-2026-07-23.md），提交 d138ead→be70e78（6个）：
  - 客观核验：flutter analyze 仍 0 issue；prefer_initializing_formals 全局关闭项已移除并重新启用（e3b00d3 修7处）；巨型文件 home 937→815 / leads_list 830→797（仅抽 *_skeletons.dart，页面级未拆）；debugPrint/TODO 全库无（仅 app.dart:17 兜底 print）
  - be70e78 重写 schedule_dialog(477行)/edit_lead_dialog(311行)：底部抽屉、ConsumerStatefulWidget+dispose+mounted、TDesign chips、日期只能选未来且提交再校验、状态前向流转 map——范本级
  - 评级：A-（第一轮）→ A
- 开放项顺延：home/leads_list 仅抽骨架(页面级未拆)、force_change/follow_up_panel P2延后未做、_build* 注释仍缺；新增 watch：login_page 601行(本Sprint未碰，既有)
- 产出：docs/review/SPRINT-REVIEW-2026-07-23.md
