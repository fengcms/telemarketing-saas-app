# 2026-07-23 开发日志

## 环境搭建（新电脑）

### 已安装
- Flutter SDK（通过 Homebrew）
- 编译 APK 成功
- 真机 Redmi K60 USB 部署成功

### 踩坑
- `tdesign_flutter 0.2.7` 与 Dart 3.12 兼容问题：`_TDIconsData extends IconData`（IconData 变为 final class）
- 修复方法：Python 脚本批量替换 2115 个图标常量为直接 `IconData(...)` 构造

## Node 1 — 线索详情页数据层

### 新增文件
- `lib/models/lead_detail.dart` — 线索详情模型（含 project/owner 内嵌对象）
- `lib/models/follow_up_record.dart` — 跟进记录模型
- `lib/models/call_record.dart` — 通话记录模型
- `lib/models/lead_list_context.dart` — 列表上下文模型（底部导航辅助）
- `lib/providers/lead_detail_provider.dart` — 线索详情状态管理器（3 个并行请求、底部导航切换、局部刷新）

### 修改文件
- `lib/services/api_constants.dart` — 新增 `calls` 端点
- `lib/services/lead_service.dart` — 新增 8 个方法（详情/跟进/通话/编辑/创建/补正/日程）
- `lib/constants/lead_constants.dart` — 新增 `answerTypeLabels` 映射

## Node 2 — 页面骨架 + 头部 + 操作按钮

### 新增文件
- `lib/pages/leads/lead_detail_page.dart` — 主页面（CustomScrollView + SliverAppBar 可折叠 + 骨架屏 + 错误态）
- `lib/pages/leads/widgets/lead_header_section.dart` — Section A（姓名+标签+电话号码+详细信息）
- `lib/pages/leads/widgets/lead_action_bar.dart` — Section B（拨号/跟进/预约/编辑 4 按钮）

### 修改文件
- `lib/pages/leads/leads_list_page.dart` — 卡片点击跳转详情页，携带 listContext

### 关键决策
- TDNavBar 在 0.2.7 版本中接受 `leftBarItems`/`rightBarItems`（TDNavBarItem 列表），不是 `leading`
- TDButton `type: TDButtonType.text` 实现纯文字按钮（不是 `theme: TDButtonTheme.text`）
- 电话拨号 FAB 使用 Material FloatingActionButton（TDFloatingActionButton 在 0.2.7 中不存在）
- 导航使用 Navigator.push（go_router 虽在 pubspec 中但未启用）

## Node 3 — 跟进时间线（Section C）

### 新增文件
- `lib/pages/leads/widgets/follow_up_card.dart` — 单条跟进记录卡片（时间/跟进人/接听类型标签+时长/内容/分类变更/编辑删除按钮）
- `lib/pages/leads/widgets/follow_up_timeline.dart` — 跟进时间线组件（圆点+连线布局/加载更多本地切片/空态/骨架屏）

### 修改文件
- `lib/pages/leads/lead_detail_page.dart` — 接入 FollowUpTimeline

### 关键决策
- 编辑按钮可见性前端计算：TE = 本人+≤5分钟，TM/TA = 全部
- 删除按钮：TE = 本人（不限时间），TM/TA = 全部
- 时间线圆点：已接听/最新=brand-7实心，未接听=gray-4空心
- 本地切片渲染（API一次返回全量，前端分 _batchSize=10 条展示）
- 跟进人姓名暂时显示 userId（后接 OptionsCacheService 解析）
- 分类变更通过与上一条记录的 categoryId 比对实现
- 编辑/删除按钮 onTap 留桩待 Node 5

## Node 4 — 通话记录摘要 + 底部导航条

### 新增文件
- `lib/pages/leads/widgets/call_records_section.dart` — Section D（标题+查看全部、3条通话记录行、接听类型标签、补正按钮TM/TA、空态）
- `lib/pages/leads/widgets/lead_bottom_nav.dart` — 底部线索导航条（上一个/计数/下一个、禁用态gray-4、仅listContext存在时显示）

### 修改文件
- `lib/pages/leads/lead_detail_page.dart` — 接入 CallRecordsSection + LeadBottomNav（bottomNavigationBar）

### 当前状态
- Node 1-4 全部完成，0 个编译错误

## Node 5a — 跟进面板 + 编辑跟进弹窗

### 新增文件
- `lib/pages/leads/widgets/follow_up_panel.dart` — 跟进面板（showModalBottomSheet）：TDTextarea 内容+字数计数、接听类型5选1（Material 自定义）、通话时长 TDStepper×2、分类选择、提交校验+API+TDToast
- `lib/pages/leads/widgets/edit_follow_up_dialog.dart` — 编辑跟进弹窗（AlertDialog）：预填内容、TDTextarea、保存API+刷新

### 修改文件
- `lib/pages/leads/widgets/lead_action_bar.dart` — "跟进"按钮连接 FollowUpPanel（新增 leadId 参数）
- `lib/pages/leads/widgets/lead_action_bar.dart` — "跟进"按钮连接 FollowUpPanel
- `lib/pages/leads/widgets/follow_up_timeline.dart` — "编辑"按钮连接 EditFollowUpDialog（新增 leadId 参数）
- `lib/pages/leads/lead_detail_page.dart` — 传递 leadId 给子组件

### 踩坑
- TDCheckTag 在 tdesign_flutter 0.2.7 中不存在，改用 Material Container 自定义选择器
- TDPopup/TDDialog 无 show 方法，改用 Flutter 原生 showModalBottomSheet/showDialog

## Node 5b — 预约弹窗 + 编辑线索 + 拨号流程

### 新增文件
- `lib/pages/leads/widgets/schedule_dialog.dart` — 预约弹窗（日期选择器TDPicker.showDatePicker、时间选择showTimePicker、备注、过去时间校验+API+TDToast）
- `lib/pages/leads/widgets/edit_lead_dialog.dart` — 编辑线索弹窗（分类从OptionsCacheService加载、状态前向流转TE限制、底部Sheet选择器、API+刷新+TDToast）
- `lib/pages/leads/widgets/dial_helper.dart` — 拨号辅助（夜间禁呼时段检查、确认弹窗、url_launcher tel:）

### 修改文件
- `lib/pages/leads/widgets/lead_action_bar.dart` — 拨号/预约/编辑按钮全部连接弹窗
- `lib/pages/leads/widgets/lead_header_section.dart` — 电话FAB连接拨号流程
- `pubspec.yaml` — 添加 url_launcher 依赖

### 修 bug
- `flutter pub get` 使用系统代理连接 pub.flutter-io.cn 超时；需 `no_proxy=* flutter pub get` 绕过代理

## Node 5c — 通话补正 + 删除确认 + 夜间禁呼

### 新增文件
- `lib/pages/leads/widgets/delete_confirm_dialog.dart` — 删除确认弹窗
- `lib/pages/leads/widgets/correct_call_dialog.dart` — 通话补正弹窗（接听类型+时长+预填）

### 修改文件
- `lib/pages/leads/widgets/follow_up_timeline.dart` — 删除按钮连接删除确认弹窗
- `lib/pages/leads/widgets/call_records_section.dart` — 补正按钮连接通话补正弹窗

### 最终状态
- 线索详情页（05）全部 5 个节点开发完成
- 全项目 0 个编译错误
- MILESTONES.md 待更新
- 还剩 Node 5（全部弹窗/面板交互）待开发

## 最终交接
- 编写 `docs/dev/HANDOVER_05_LEAD_DETAIL.md` 列出未完成项
- P0（功能缺失 5 项）：通话反馈面板、「查看全部」跳转、跟进面板分类选择器、夜间禁呼配置、onResume 反馈
- P1（UI/UX 6 项）：圆点连线高度固定、结束时间缺失等
- P2（优化 5 项）：预加载策略、骨架动画等
- git commit & push
