# 2026-07-26 工作日志

## 修复：web 上「登录正常、进首页不发包」崩溃

### 现象
`flutter run -d chrome --dart-define=DEV_TOOLS=true` 登录后首页不发包；安卓真机正常。
最初误判为 CORS（Authorization 头预检），用户贴出 Console 红报错后推翻。

### 根因（Riverpod 构建期改 provider）
- `lib/providers/schedule_stats_provider.dart` 的 `ScheduleStatsNotifier.load()`
  第一行是**同步** `state = state.copyWith(isLoading: true)`。
  notifier 构造器里调用了 `load()`，且 `profile_page.dart` 的 `initState` 也同步调了 `.notifier.load()`。
- `MainShell` 用 `IndexedStack` 一次性挂载 4 个 Tab，进首页时 `ProfilePage.initState`
  触发该同步 setState → Riverpod `_debugCanModifyProviders` 抛错 → **整个 MainShell 构建崩溃**，
  `HomePageNotifier.loadData()` 没机会跑 → 表现为「首页不发包」。
- 仅 web 暴露：debug 构建期保护在原生端时序差异下未触发。

### 修复
1. `schedule_stats_provider.dart`：删除 `load()` 首行同步 setState，初始 loading 态改到
   构造器 `super(const ScheduleStatsState(isLoading: true))`。此后 `load()` 只在 `await` 后 setState，合法。
2. `profile_page.dart`：删掉 `initState` 里多余的 `ref.read(scheduleStatsProvider.notifier).load()`
   （notifier 构造已自动 load，避免重复请求）。
- `flutter analyze` 通过，无报错。

### 排查要点（经验）
- Riverpod 仅拦截 **build/initState/didChangeDependencies 等构建期**的 provider 修改；
  事件回调（onRefresh/按钮）里调 `.notifier.refresh()` 安全。
- 全仓扫描：其余 `notifier.refresh()` 均在事件回调中，无同类隐患。
- 首页 `_safeCall` 吞异常导致此类问题静默，建议开发期加 `debugPrint`。

## 二次排查：崩溃修好后仍「进首页不发包」
- 现象：红报错消失，但登录后首页不发请求；控制台重复 `Selected call is null`（来自 alice-1.0.0
  `alice_memory_storage.dart` 的 `addError/addResponse` 找不到对应 call）。
- 结论方向：Alice 拦截器确实收到请求（`onRequest` 已 addCall），但请求最终以错误结束且
  `error.requestOptions.hashCode` 匹配不到原 call → 请求被浏览器层拦（疑似 CORS 对 `Authorization`
  头预检失败，登录为 public 接口不带 auth 头故正常）。
- 根因尚未 100% 坐实（用户在 web 上「确认 CORS 没问题」可能只验了登录/public 接口）。
- 措施：`lib/services/api_client.dart` 的 token 拦截器加 `enableDevTools` 守卫的 `[API][REQ]/[RESP]/[ERR]`
  debugPrint（正式包编译期消除），让用户重跑 web 抓真实错误（type/msg/status/auth 头）。
- 待用户回贴控制台 `[API]` 行确认根因（CORS vs 401 vs 网络）。

## 最终根因（已坐实，非 CORS）
- 用户回贴 `[API]` 日志：所有非 public 请求 `[API][REQ] ... auth=false`，且 `[API][ERR] type=unknown msg=null`，
  网络面板只有 login。**login 是 public 接口、拦截器里 `_isPublicEndpoint` 提前 return 不碰 token 存储**，
  所以只有带 token 的请求才崩 → 根因是 web 上 `getAccessToken()` 抛异常，请求在注入 token 前被拦截器中止。
- 深挖：`flutter_secure_storage_web` 1.2.1 的 `read` 走 AES-GCM 解密，强依赖 `window.crypto.subtle`；
  用户 dev server 用 HTTP 非 localhost 源（不安全上下文）→ `crypto.subtle` 为 undefined → 抛异常。
  `options['publicKey']` 默认有值（`_selectOptions` 在 web 返回 `webOptions.params`），故非 `!` 问题。
- 修复：
  1. `lib/services/token_storage.dart` 改为 web 走 `shared_preferences`（localStorage，无 crypto 依赖），
     原生继续 `flutter_secure_storage`（Keystore）。`shared_preferences` 已在依赖（^2.3.0）。
  2. `lib/services/api_client.dart` 的 token 读取加 try/catch：读取失败只降级为不带鉴权发出，
     不再中止请求（避免再次静默失败）。
- 验证：`flutter analyze` 两文件 0 error（仅 11 个 kIsWeb 下 `!` 的误报 warning，不影响构建）。
- 待用户重跑确认 `[API][REQ] auth=true` 且 `[API][RESP] status=200`。
- 注意：因之前 web 端 token 从未成功持久化，修复后需重新登录一次。

## 收尾：清理调试日志 + 落盘踩坑文档
- 用户确认 web 登录后数据正常（`[API][RESP] 200`），根因闭环。
- 清理 `lib/services/api_client.dart` 的 `[API][REQ]/[RESP]/[ERR]` 三处 `enableDevTools` 守卫调试日志
  （Alice 已覆盖抓包，属冗余）。**刻意保留** `[API][TOKEN-ERR]` 并去掉守卫改为常驻——
  它是 catch 块里唯一能暴露 token 读取失败（如 web 存储异常）的出口，常驻打印对生产可观测性有价值，
  避免再次被 `_safeCall` 静默吞掉。
- `flutter analyze lib/services/api_client.dart` → No issues found。
- 踩坑落盘：`docs/dev/DEVELOPMENT_PITFALLS.md` 追加 `12.2`（Riverpod 构建期改 provider/web 红崩、
  IndexedStack 连带打崩壳）与 `12.3`（flutter_secure_storage_web 非安全上下文 crypto.subtle 不可用 →
  token 读取抛异常 → 拦截器中止请求 → 静默失败，含 web 用 shared_preferences 分流 + 拦截器 try/catch 修复）。
- 文档「最后更新」日期同步为 2026-07-26。
- 安全权衡：token 在 web 用 shared_preferences 是明文 localStorage，弱于 Keystore，仅适合调试/内测；
  正式发布 web 需评估（HTTPS + 短期 token + refresh 兜底，或后端 httpOnly Cookie）。

## 分析（未改码）：通话记录 / 客户列表 筛选条 UI 一致性
- 用户要求分析两页「搜索框下方筛选项」风格不一致，仅出报告不改码。
- 产物：`docs/dev/FILTER_BAR_ANALYSIS-2026-07-26.md`。
- 核心发现：两筛选条在 8 维度不一致——
  1) 交互范式：通话记录=横滚胶囊 chip，客户列表=通栏分段控件；
  2) 选中态：通话记录浅蓝底+蓝边，客户列表仅浅蓝底无边框；
  3) 未选中背景：灰底 vs 透明；
  4) 形状：全圆药丸(999) vs 圆角矩形(6)；
  5) 选中字重：常规 vs w500；
  6) 硬编码 `#6B7A90`（调色板外，应换 BrandColors.textSecondary）+ 其它色值未用 BrandColors；
  7) 内边距 16/4/12 vs 12/6/10（横向 12 违反 16 页边距约定）；
  8) 搜索↔筛选分隔线：无 vs 有。
- 根因：缺公共筛选条组件，各页自造轮子；晚于 TagChip/UI_STYLE_GUIDE 规范。
- 推荐方案：抽 `AppFilterChips`，统一「横滚药丸 + 浅蓝选中(#F2F3FF)+蓝边+蓝字+灰底未选中」，
  全量用 BrandColors，搜索↔筛选两页统一加 1px BrandColors.line，点击改用 InkWell。
- 待用户确认后派活执行。

## 落地（方案 B 已执行）：筛选条统一
- 用户确认方案 B，已落地改造，未改任何业务逻辑。
- 新增 `lib/widgets/app_filter_chips.dart`（`AppFilterChips` + `FilterChipItem`），统一横滚药丸筛选条：
  全圆药丸(999)、选中浅蓝底 primarySurface+1px 蓝边+蓝字 w500、未选中灰底 surface+textSecondary、
  高32、横向padding14、内边距 fromLTRB(16,8,16,8)、InkWell+Ink 水波纹、全量 BrandColors。
- `CallFilterBar` / `CustomerFilterBar` 重写为直接返回 `AppFilterChips`（客户列表从分段控件改为横滚药丸，
  call 原即横滚）。回调签名不变。
- 两页搜索↔筛选均加 1px `BrandColors.line` 分隔线（call 补、customer 改硬编码→line）；两页加 color_scheme 导入。
- 修复文档漂移：`color_scheme.dart` 补 `BrandColors.line = #EEEEEE`（UI_STYLE_GUIDE §2.1 有记录但代码缺失）。
- `flutter analyze`：本次 6 文件 0 issue；全仓仅余 token_storage.dart 11 个 `!` 误报 warning（web 存储修复遗留，无关）。
- UI_STYLE_GUIDE §3 登记 AppFilterChips + 规则「列表页筛选条必须用 AppFilterChips，禁止自绘」。
- 待用户真机热重启核对两页筛选条观感。

## 补充修复：筛选条默认占满 100% 宽（已推仓库）
- 用户反馈方案 B 落地后，筛选栏选项少时未占满屏幕 100% 宽，两侧露页面灰底留白，要求默认占满。
- 根因：AppFilterChips 原 `Container` 未显式强制宽度；且 `SingleChildScrollView`+`Row` 在内容少时左对齐、右侧留白。
- 修复（仅改 `lib/widgets/app_filter_chips.dart`）：`Container` 加 `width: double.infinity` 强制白底占满全屏；
  内部 `LayoutBuilder` 测可用宽——选项能放下 → `Row(spaceBetween)` 沿行铺满（无两侧留白），超出 → `SingleChildScrollView` 横滚；
  文字宽用 `TextPainter` 实测（w500 近似、尊重 `textScaler`）。
- 效果：客户列表(5项)小屏铺满；通话记录(6项)窄屏横滚、宽屏铺满。
- `flutter analyze` 0 issue；报告追加「十一、补充修复」；UI_STYLE_GUIDE §3 AppFilterChips 描述补「默认占满100%宽」。
- 已与方案 B 改动一并 commit & push 到 master。

## 分析（未改码）：日程列表页 可抽象组件 / 优化项
- 用户满意当前视觉，要求分析可抽象公共组件与其他优化项，仅出报告不改码。
- 产物：`docs/dev/SCHEDULE_LIST_ANALYSIS-2026-07-26.md`。
- 关键发现：
  1) 错误态 `_buildError` 完全自绘，项目已有公共 `AppErrorBody` 应复用（建议扩 `title` 参数支持两级文案）；
  2) 空态自绘，建议新增 `AppEmptyBody` 统一三列表；
  3) 状态标签 `_StatusTag` 与公共 `AppTag` 同构应复用；卡片时间/归属人行可复用 `AppInfoRow`（label 改可选）；
  4) 双 Tab 栏 / 我的·团队切换 / 两个吸顶头(90%同构) / 吸顶委托 / 列表 footer 均可抽公共组件；
  5) 颜色硬编码泛滥：野色 `#6B7A90` 再现（与筛选条同问题），语义色 `#D54941/#00A870/#E37318` 未进 BrandColors；建议补 danger/success/warning 并全量替换；卡片阴影三处参数不一致建议统一 token；
  6) 维护性：分组 logic 用 part 文件建议改独立模块；滚动触底节流、`ScheduleCard` 为 ConsumerWidget 的 provider 数量等。
- 全部建议视觉零变化，分四批落地（规范清零→消重复→抽新组件→维护性）。

## 执行：日程列表页 第一批+第二批（已改码，未提交，待真机验证）
- 按报告分批落地。第一批（颜色集中化）+ 第二批（复用公共组件）已完成，`flutter analyze` 6 文件 0 issue。
- 公共组件扩展（默认值保持原外观，不影响现有调用方）：
  - `AppErrorBody` 加可选 `title`/`iconSize`/`iconColor`/`messageColor`；
  - `AppTag` 加可选 `padding`（默认 6×2）。
- 颜色：`schedule_list_page`/`schedule_card`/`schedule_date_header`/`schedule_overdue_header` 硬编码色替换为 BrandColors；
  野色 `#6B7A90`→`textSecondary`（**轻微变浅**，按方案，若想保留原深色需新增强色 token）。
  保留字面量：`#00A870`（完成绿，设计系统双绿不一致，跨应用问题不在此页范围）、`#DCDCDC`（图标，第三批 AppEmptyBody 统一）。
- 复用：`_buildError`→`AppErrorBody`；删除 `schedule_card._StatusTag` 改用 `AppTag`。
- **跳过**：`AppInfoRow` 复用卡片时间/归属人行——其为详情页风格(16px图标+`标签:值`+textPrimary)，与卡片紧凑 14px 不兼容，强行复用改观感，保持自绘。
- 报告 `SCHEDULE_LIST_ANALYSIS-2026-07-26.md` 追加「七、执行记录」。
- 未提交：按项目约定「开发完成要求真机实测」，待用户验证后 commit。第三批（AppEmptyBody/AppSegmentedTab/AppScopeToggle/吸顶头合并）、第四批（维护性）待续。
- 注：`schedule_search_page`/`schedule_detail_*`/`schedule_form_fields` 仍含硬编码色，属列表页分析范围外，未动。

## 执行：日程列表页 第三批 + 第四批（抽组件部分）已 commit & push（7b2b672）
- 第三批抽新组件：
  - 新增 `AppEmptyBody`（空态，颜色收口 #DCDCDC/#181818/#A6A6A6 → 组件内 textDisabled/textPrimary/textSecondary；图标灰 #DCDCDC 保留字面量）；
  - 新增 `AppSegmentedTab`（带计数徽标+下划线分段 Tab）；
  - 新增 `AppScopeToggle`（我的/团队药丸，含 ScopeOption）；
  - 合并 `schedule_date_header` + `schedule_overdue_header` → `schedule_sticky_header.dart` 的 `ScheduleStickyHeader(title, icon?, iconColor?, onTap?)`，删两个旧文件。
- 第四批抽组件部分：
  - 新增 `FixedStickyHeaderDelegate`（app_sticky_header.dart），删日程页私有 `_StickyHeaderDelegate`；
  - 新增 `AppListFooter`（加载更多/已加载全部/留白三段式）。
- 所有新 widget 补 `library;` 指令（对齐项目 16 个 widget 惯例），消除 dangling_library_doc_comments info。
- `flutter analyze` 7 改动文件 → No issues found；全仓仅 `token_storage.dart` 既有 11 个 `!` warning（误报，无关）。
- `UI_STYLE_GUIDE` §3 表格追加 5 组件 + 规则「列表页空态/Tab/作用域切换/吸顶委托/footer 必须用公共组件」。
- 第四批维护性项（part→独立 module、loadMore 守卫确认、userNameProvider 上移）**已落地**（用户确认继续）：
  - `schedule_grouping.dart` 由 `part of` 转独立 `library`，`_Group`→`ScheduleGroup`、`_groupSchedules`→`groupSchedules` 公开；列表页改 `import`。
  - `loadMore` 守卫核实已存在（isLoadingMore/hasMore/isInitialLoading/cached 四重 + _generation 代际），不改码。
  - `ScheduleCard` 由 ConsumerWidget 降为 StatelessWidget（加 ownerName），姓名改列表页统一 watch `userNameProvider` 解析 Map 下发；搜索页同步注入避免行为回退。
  - `flutter analyze` 4 文件 → 0 issue；全仓无新增 error/warning。

## 分析（未改码）：日程详情页 UI 状况
- 用户表示对详情页 UI 不满意，要求分析，仅出报告不改码。
- 产物：`docs/dev/SCHEDULE_DETAIL_ANALYSIS-2026-07-26.md`。
- 核心发现（详情页是本轮整改唯一漏清洗的展示页）：
  - **P0 视觉离群**：分区标题/字段用 emoji（📅👤📝📞+🔄），全仓仅此处用 emoji，其余页（含线索详情）均 Material Icons，源自 TDesign 旧稿未改。
  - **P0 视觉跳变**：详情卡圆角12无阴影、列表卡10+阴影、骨架屏10+阴影 → 加载完成卡片「缩角+掉阴影」闪跳。建议详情向列表对齐为 10+阴影。
  - **P1 跨页不一致**：状态标签手写 `statusTag` 未复用 `AppTag`；信息卡手写 `_infoRow` 未复用 `AppInfoRow`；错误态手写 `_buildErrorState` 未复用 `AppErrorBody`（列表页已复用）。
  - **P2 主题合规**：约 34 处硬编码 `Color(0xFF...)` 未收敛（cards:23/page:7/actions:4/skeleton:4）；Scaffold 背景未用 `BrandColors.surface`。
  - **澄清**：完成态双绿——列表 `#00A870` vs 详情 `#2BA471`(=BrandColors.success)，详情侧与主题一致，列表是离群方，统一应改列表（不在本页范围）。
  - **已合规**：分区标题灰色非品牌蓝、AppBar 品牌色、底部栏 AppActionBar、标题字号24 与线索详情一致。
- 建议分三批：B1 emoji→Icons + 卡片圆角/阴影统一（纯视觉）→ B2 复用 AppTag/AppInfoRow/AppErrorBody → B3 硬编码色集中化。
- 待用户确认 3 点：信息卡版式(A 复用 AppInfoRow 加图标 / B 保留左标签右值)、卡片圆角(详情向列表对齐10+阴影)、状态标签(复用 AppTag 并与列表共享 `_statusColor`)。

## 执行（已改码未提交，待真机验证）：日程详情页 UI 三批整改
- 用户确认「按你的计划推进，仅 UI 不破坏业务」。三批全落地，`flutter analyze` 全仓 0 错误（仅 token_storage.dart 既有 11 个 `!` warning 无关）。
- Batch 1：emoji→Icons（新增 `_sectionLabel` 助手；📅👤📝→`Icons.event/person/description`，📞→`Icons.phone` 前导；操作栏 🔄→`Icons.replay`）；`detailCard` 圆角 12→10 + 加 `0x0D000000` 阴影，对齐列表卡/骨架屏，消除加载闪跳。
- Batch 2：状态标签 `statusTag`→复用 `AppTag`（颜色映射保持原值）；信息卡 `_infoRow`→复用 `AppInfoRow`（图标 schedule/badge_outlined/update，对齐线索详情，删 `_infoRow`）；错误态 `_buildErrorState`→复用 `AppErrorBody`。
- Batch 3：硬编码色集中化——`schedule_detail_page`(F3F3F3→surface、D54941→error)、`schedule_detail_cards`(A6A6A6→textSecondary、181818→textPrimary、完成fg→success、取消/待办 bg→surface/primarySurface、fg→textSecondary/primary、逾期→error)、`schedule_detail_actions`(EEEEEE→line、0052D9→primary、181818→textPrimary)、`schedule_skeleton` 共享(E7E7E7×4→border)。保留内联：E3F3EA(完成标签浅绿底)/DCDCDC(禁用灰)/F4F4F4(shimmer)/阴影遮罩。
- **未动**（报告明确范围外）：列表 `_statusColor` 的 `#00A870` 双绿、search_page/form_fields 硬编码色。
- 报告 `SCHEDULE_DETAIL_ANALYSIS-2026-07-26.md` 追加「六、执行记录」。
- 按项目约定，开发完成需真机热重启核实后再 commit & push。

## 完成（已提交）：日程详情页 UI 整改
- 用户真机核实「视觉 OK」→ 进入收尾：写进度文档 `PROGRESS_SCHEDULE_DETAIL_UI-2026-07-26.md`；`UI_STYLE_GUIDE.md` §14 升 v2.1 补图标禁令(禁止 emoji)/`AppTag`/`AppInfoRow`/`AppErrorBody` 复用/日程卡片 10px+阴影；`DEVELOPMENT_PITFALLS.md` §13 记「逐页迁移漏掉同模块兄弟页」+ 双绿澄清（详情页 #2BA471=success 正确，列表页 #00A870 离群）。
- 提交 `aeea5e9` 推 `master`：9 文件 +523/−125（4 源码 + 4 文档 + 1 日志）。
- 业务逻辑零改动，已确认。

## 完成（待提交）：日程搜索页 / 表单页 硬编码色收尾
- 用户指示「按你的计划去 schedule_search_page / schedule_form_fields 收尾硬编码色」。纯色值集中化，零视觉变化、零业务改动。
- `schedule_search_page.dart`：0052D9→primary、F3F3F3→surface、181818→textPrimary、A6A6A6→textSecondary、C5C5C5→textDisabled（加 color_scheme 导入）；保留内联 2 处 `0xFFDCDCDC`（错误/空态禁用灰图标，无对应常量）。
- `schedule_form_fields.dart`：181818→textPrimary、A6A6A6→textSecondary、E7E7E7→border、D54941→error；导入在父文件 `schedule_form_sheet.dart` 添加（form_fields 为 part of）。
- `flutter analyze` 全仓 0 错误（仍仅 token_storage.dart 既有 11 个 `!` warning 无关）。
- 用户真机核实「视觉 OK」→ 写进度文档 `PROGRESS_SCHEDULE_SEARCH_FORM_COLORS-2026-07-26.md`，随后 commit & push。
- **全模块硬编码色整改至此全部完成**：列表页(f058760/4f37afd/7b2b672/3bbc02d) + 详情页(aeea5e9) + 搜索页 + 表单页。

## 线索列表 / 公海列表 UI 整改（LEADS_LIST）
- 分析报告 `LEADS_LIST_ANALYSIS-2026-07-26.md`（P0 跨模块圆角不一致+骨架无阴影闪跳 / P1 未复用公共组件 / P2 硬编码色；另发现公海卡分类写死'商铺'功能性 bug）。
- 用户「按你的计划处理吧」→ 执行三批纯 UI 整改，公海 bug 不在 UI 范围未动（待单独授权）。
- 改动文件：leads_list_page(圆角阴影骨架+复用AppEmptyBody/AppErrorBody/AppListFooter+色值)、lib/widgets/lead_card(圆角10+色值)、public_lead_card(圆角+line+warning+色值)、leads_top_bar(primary/error)、leads_skeletons(border)。
- 关键常量发现：`BrandColors.warning=0xFFED7B2F`（公海状态橙正好有）、`line=0xFFEEEEEE`（分割线）、`border=0xFFE7E7E7`（骨架灰块）。
- `flutter analyze` 5 文件 No issues，全仓 0 新增（仍 token_storage.dart 11 warning 无关）。业务逻辑零改动。
- 进度文档 `PROGRESS_LEADS_LIST_UI-2026-07-26.md`；报告追加 §七执行记录。**待用户真机验证后 commit**（按项目约定开发完需真机核对）。
- 线索模块其他子页（filter_sheet/edit_dialog/follow_up_*/detail/bottom_nav/dial_helper/correct_call_dialog）硬编码色属延伸范围，本轮未动。

## 公海线索卡片优化（PUBLIC_LEAD_CARD）
- 方案 `PUBLIC_LEAD_CARD_PLAN-2026-07-26.md`；用户确认：按钮上移头行 / 不加提示 / 一并修「商铺」bug。
- `public_lead_card.dart` 整体重写：StatelessWidget→ConsumerWidget（接 optionsCacheProvider）；去 GestureDetector/onTap（卡片不可点进详情）；领取按钮上移右上角+去底部条+分割线；电话+时间合并单行；_buildCategoryRow 改用 getCategoryName/getProjectName 解析真实分类（修写死'商铺'）。
- `leads_list_page.dart:526` 删除 PublicLeadCard 的 onTap→Navigator.push(detail)（保留 onClaim）；LeadDetailPage import 仍被「我的线索」卡使用未删。
- `flutter analyze` 公海卡+列表页 No issues；全仓仍仅 token_storage.dart 11 warning 无关。领取业务逻辑零改动。
- 进度文档 `PROGRESS_PUBLIC_LEAD_CARD-2026-07-26.md`；方案追加 §六执行记录。**待用户真机验证后 commit**（按项目约定）。

## 排查（已修复）：设置页 ListTile 水波纹 assertion + web 字体/insets 警告
- A（真实代码问题，已修）：`lib/pages/settings/settings_page.dart` 的 `_buildCard` 用 `Container(decoration:)`（白底+边框+圆角）包 `ListTile`。
  ListTile 背景/水波纹画在最近 Material 祖先上，被中间 DecoratedBox 挡住 → Flutter 抛
  `ListTile background color or ink splashes may be invisible`。`_buildCard` 内全部 ListTile（修改密码/退出登录/全设备退出/关于）都触发。
  修法：外层保留 `Container(margin)`，内层 `Container(decoration)` 换成 `Material(type:card, elevation:0, shape:RoundedRectangleBorder(...))`，
  使 Material 成为最近带背景祖先，水波纹正常。`flutter analyze` 通过。
- B（框架/环境，无需改）：`ViewInsets cannot be negative` 来自 Flutter Web 引擎 `_handleBrowserResize` 计算键盘 insets 负值（键盘收起时）。
  特定 Flutter 版本 web 已知断言，通常无害、不影响运行；非业务代码问题。
- C（网络/资源，可选）：`fonts.gstatic.com/notosanssc/...woff2 net::ERR_TIMED_OUT` 是 gstatic 访问超时（网络），
  导致中文字形回退、控制台 `Could not find a set of Noto fonts...`。可选修法：把 Noto Sans SC 本地化到 assets + pubspec `fonts`，脱离 CDN。待用户决定。

## 公海骨架溢出修复 + 日程搜索栏统一（HOTFIX）
- 用户反馈：① 公海搜索时骨架屏底部有「越界英文提示」；② 日程搜索页搜索组件疑似未用公共 AppSearchBar。
- ① 根因：`leads_list_page` 的 PublicSkeleton 卡片固定 height 160，但 5 灰块+36px 按钮块内容 146 > 可用 128（160-32 padding） → Column 垂直溢出 18px，调试模式显示黄黑 overflow 横幅。修复：去固定 height，改 Row+Expanded 信息列+领取按钮占位（84x32），镜像真实 PublicLeadCard 结构，消除溢出与加载闪跳；margin 12→10 对齐真实卡。
- ② 确认属实：schedule_search_page 手写搜索栏（缺聚焦边框+点按聚焦），替换成公共 AppSearchBar（controller/onSearch:_doSearch/hintText/keyboardType:phone），加 import app_search_bar。线索列表/通话记录已用 AppSearchBar，现三处一致。
- `flutter analyze` 两文件 No issues；全仓仍仅 token_storage.dart 11 warning 无关。纯 UI，业务逻辑零改动。
- **待用户真机验证后 commit**（按项目约定）。

## 公海领取按钮改圆形图标（用户真机实测反馈）
- 用户真机实测反馈两点：① 点领取 loading 时胶囊按钮宽度收窄→左侧信息列重排「弹到右侧」；② 领取按钮与姓名/状态行顶对齐，有视觉错位。
- 用户提案（已采纳）：领取按钮改为右侧上下居中的圆形主题色图标，loading 仅中心图标换 spinner。
- 实现（`public_lead_card.dart`）：
  - `Row` 由 `CrossAxisAlignment.start`→`center`（按钮相对整列上下居中，消错位）。
  - `_buildClaimButton` 重写：固定 44×44 `Ink`+`InkWell`(customBorder:CircleBorder) 圆形，`BrandColors.primary` 填充 + 浅阴影；图标 `Icons.person_add`（语义=把该联系人领为己有）白色 22；loading 时仅中心换 `CircularProgressIndicator(strokeWidth:2.5, white)`，**宽高固定不变→零抖动**；外层 `Tooltip('领取该线索')`；onClaim==null 时转 `textDisabled` 灰且不可点，claiming 时保持 primary（忙碌非禁用）。
  - 弃用 `withOpacity`→`withValues(alpha:)`，消除 2 条 deprecation info。
- 骨架屏镜像（`leads_list_page._buildPublicSkeleton`）：占位块 84×32 胶囊→44×44 `BoxShape.circle`，Row 对齐同步改 center，保持 loading 态一致。
- `flutter analyze` 两文件 → No issues found。纯 UI，业务逻辑零改动。

## 修复：团队员工筛选漏显示角色（OptionItem 未解析 role）
- 现象：用户贴出 `/options/users` 返回每条带 `role`（tenant_admin/tenant_manager/tenant_employee），
  但团队日程的员工筛选只显示颜色圆点+姓名，不显示角色。质疑是否本地映射漏取 role。
- 排查（读源码坐实，非猜）：`lib/models/option_item.dart` 的 `OptionItem` 仅 `id/name/type` 三字段，
  `fromJson` 只取这仨，`role` 被静默丢弃；`options_cache_service.getUsers()` 走 `OptionItem.fromJson` → role 从头就没接住。
  **非字段改名**（接口字段一直是 `role`）。
- 修复（用户确认「补 role 并展示」）：
  1. 新增 `lib/theme/role_label.dart` 共享 `roleLabel(String?)`（tenant_employee→电销专员 / tenant_manager→团队经理 / tenant_admin→管理员，其余→''），
     抽出来是因为 `profile_page._roleLabel` 已有同款映射，避免两处硬编码不一致；
  2. `OptionItem` 加 `final String? role;`（const 构造器加 `this.role` 可选参，非破坏性），`fromJson` 补 `role: json['role']?.toString()`；
  3. `profile_page` 删私有 `_roleLabel`、改 import 共享函数；注意局部变量原叫 `roleLabel` 会与导入函数重名 → 改名 `roleText`；
  4. 日程列表 `_MemberTile` 加 `role` 参数，筛选项显示「姓名 · 角色」（12px 灰，role 空则不显）。
- `flutter analyze` 本次 4 文件（role_label/option_item/profile/schedule_list）→ 0 issue；全仓仍仅 token_storage.dart 11 个 `!` warning 无关。
- 经验：跨接口共用的 `OptionItem` 增字段务必 nullable，避免非 user 接口（分类/项目）无 role 时报错；角色文案统一走 `roleLabel()`。
- 已随 v0.24 一并发构建装真机（Iw7gyL，含 DEV_TOOLS 调试浮标），待真机验证后写进度文档+commit。
- **待用户真机验证后 commit**（按项目约定）。

## v0.24 收尾完成（2026-07-26 末）
- commit：`9dbde3e feat(team-schedule): 团队日程视图 + 角色标签修复（v0.24）`，12 文件 / +700 -34，已 push 到 origin/master。
- `docs/dev/MILESTONES.md` 补 **v0.24 节点**（此前因 em dash 锚点字符不匹配失败，改用普通连字符锚点插入成功）。
- 团队模块现状：阶段一✅（团队线索池 v0.22）+ 阶段二✅（团队日程视图 v0.24）；仅阶段三 **v0.25 团队统计独立页** 未开发（需引入 fl_chart）。
- 遗留：KGP 告警按方案 A 暂接受；v0.23 / v0.25 / v0.26 节点未补录到 MILESTONES（非功能阻塞）。

## v0.25 完成（团队统计独立页，2026-07-26 末）
- 用户真机实测「无明显问题」→ 进入收尾：写进度文档 `PROGRESS_TEAM_STATS-2026-07-26.md`；`DEVELOPMENT_PITFALLS.md` 加 §14（fl_chart 0.70 破坏性 API：swapAnimationDuration/Curve→duration/curve、withOpacity→withValues(alpha:)；团队统计后端 seed 聚合键 pool→pending 全链路修正、客户端统一 pending 单键不兼容 pool）。
- 团队模块三阶段全完成：v0.22 线索池 / v0.24 日程视图 / v0.25 统计页。
- `flutter analyze` 本次 13 文件 No issues；全仓仍仅 token_storage.dart 11 个 `!` warning 无关。
- 构建 `app-release.apk` 59MB（DEV_TOOLS 浮标），`adb install -r` 到 Redmi K60(`3e06fd6d`) Success。
- 提交 → commit & push origin/master（已执行：735c355）。MILESTONES 补 v0.25 节点（v0.23 仍待补）。
- 遗留：KGP 告警方案 A 暂接受；MILESTONES 的 v0.23 节点未补（非阻塞）。

## 首页/个人中心 入口接线修复（v0.31，2026-07-26 末）
- 用户实测 v0.25 通过后，选「首页/个人中心 占位接线」快修方向。
- 修复两处漏接线：首页快捷「通话记录」→ CallRecordsPage（v0.17）；首页 AppBar「团队看板」(TM/TA) → TeamStatsPage（v0.25）。
- 个人中心「我的业绩」按选择保持 ComingSoon（个人统计整页未建，属另一功能）。
- 改动：home_quick_entry_section.dart + home_page.dart 两文件换 import + 改 push 目标；analyze 2 文件 0 issue。
- 构建 app-release.apk 59MB + adb 装 Redmi K60；用户真机实测「两条入口跳转正常」。
- 进度文档 PROGRESS_HOME_ENTRY_WIRING-2026-07-26.md；MILESTONES 补 v0.31 节点。
- 全仓 ComingSoon 残留：仅 profile_page 的「我的业绩/个人统计」入口（个人统计整页未建）。

## v0.32 个人统计独立页（2026-07-26 收尾）
- 用户真机实测「测试通过」→ 进入收尾：写进度文档 `PROGRESS_PERSONAL_STATS-2026-07-26.md`；踩坑 `DEVELOPMENT_PITFALLS.md` 加 §15（骨架屏 `const Expanded(child: _box(...))` 报 `const_eval_method_invocation`——`_box` 是方法非构造器，去掉 `Expanded` 上的 `const`，全局 `const _box→_box` 替换无效因 `const` 标在 Expanded 前）；MILESTONES 补 v0.32 节点 + v0.31 待开发划掉 + 版本号 v0.32/2026-07-26。
- 接口疑云消除：用户在 AskUserQuestion 直接贴真机抓包 JSON（`myFollowed:6,myAnswered:1,...`）证实 5 区间字段齐全，按 PLAN_32 直接建页，无需微调模型。
- 转化率口径：直接展示后端 `myConversionRate`，前端不二次计算（与团队统计不同）。
- 复用：HomeService（fetchPersonalStats，同 statsMine 端点，非新建 Service）+ personal_stats_provider（5 分钟按范围 key 缓存 + sentinel，同构 team_stats_provider）。
- 改动文件：新建 personal_stats.dart / personal_stats_provider.dart / personal_stats_page.dart + widgets(date_range_selector/today_overview/detail_grid/conversion_ring) 4 个 / PLAN_32 / PROGRESS_PERSONAL_STATS；修改 home_service.dart(扩展) / profile_page.dart(入口) / profile_stats_card.dart(onTap 由调用方传 PersonalStatsPage)。
- `flutter analyze` 个人统计相关 6 文件 0 issue；全仓仅 token_storage.dart 11 个 `!` 基线 warning（web 存储修复遗留，无关）。
- 构建 `app-release.apk` 62.3MB（DEV_TOOLS 浮标），`adb install -r` Redmi K60(`3e06fd6d`) Success。
- 待 commit & push（用户要求推仓库）。
- 全端 ComingSoon 残留：个人统计整页已建 + 入口全接线，无残留占位。

## v0.33 线索筛选「归属人」角色可见性修复（2026-07-26）
- bug：员工（tenant_employee）登录后，线索列表筛选抽屉里出现「归属人」选项，但后端该角色 scope 固定 'mine'，该筛选无意义且与数据口径矛盾。
- 根因：leads_filter_sheet.dart 的 `_buildOwnerSection()` 对所有角色无条件渲染，未与后端 scope 语义对齐。
- 修复：ads_filter_sheet.dart `build()` 取 `authProvider.user.role`，仅 `tenant_admin`/`tenant_manager` 渲染「归属人」区块（连同间距包进 `if (canFilterByOwner) ...[]`）；ads_list_page.dart 激活筛选标签栏「归属」标签加 `isManager` 判定（传参 `_buildFilterTags(state, isManager)`），与抽屉一致。
- `flutter analyze` 2 文件 0 issue；构建 app-release.apk 59MB(DEV_TOOLS) 装 Redmi K60；用户真机实测「测试通过」（员工无归属人、TM/TA 正常）。
- 文档：踩坑 §16（角色可见性 + scope 语义教训）、PROGRESS_LEADS_OWNER_FILTER_ROLE-2026-07-26.md、MILESTONES 补 v0.33 节点 + 版本号升 v0.33。
- 提醒：任何「归属人/成员/团队维度」筛选或字段都要先对齐当前角色 scope（员工=mine / TM·TA=all）。

## 个人统计页 UI 微调（第二轮，改码未提交，待真机验证）
- 用户真机反馈三点：① 间距 OK；② 数字靠下 + 今日概况 Bottom overflowed by 15px、数据详情 by 5.8px；③ 转化率环太小。
- 根因（同因）：两个网格 `childAspectRatio:2.0` 强制卡片高度 < StatCard 内容实际高度（默认 padding16+label+值），内容溢出即黄黑横幅；数字「靠下」是溢出连带症状（内容被顶到格子下沿）。
- 修复：
  - `stat_card.dart` 默认 padding `all(16)`→`sym(horiz14, vert12)`、标签-值间距 8→6（释放垂直空间，内容变矮）。
  - `detail_grid.dart` ratio 2.0→1.85；`today_overview.dart` ratio 2.0→1.6（值保持30，层级不变）。
  - `conversion_ring.dart` 环 92→120、stroke 10→12、中心标签12→13。
- `flutter analyze` 4 文件 No issues found；纯 UI，业务逻辑零改动。
- **待用户真机验证后 commit**（连同前轮个人统计 StatCard 重构/日期选择器/微调一并落 PROGRESS_PERSONAL_STATS_UI 文档）。

## 个人统计页 UI 微调（第三轮，改码未提交，待真机验证）
- 用户真机反馈两点：① 转化率环仍太小（前轮 120），要求双倍甚至三倍大；② 切换时间范围无骨架屏，等接口返回后瞬间变化。
- ① 根因：前轮 120 保守 + 左右布局在手机宽度下容不下更大环。改为**上下居中布局**：环放大到 240（=120 双倍），百分比(36/primary)置于环心、「转化率」标签在下、下方「转化 X / 线索 Y」说明居中；`LayoutBuilder` 封顶 240 且随卡片可用宽自适应收窄（窄屏 320 不溢出）。`conversion_ring.dart` 重写。
- ② 根因：provider `load()` 切范围时已 `isLoading=true`，但页面 `_body` 仅 `s.stats==null` 才显骨架；切范围时 `stats` 为非 null 旧数据 → 直接显示旧数据、接口返回瞬间替换，看不到骨架。修法：`personal_stats_page._body` 判定由 `s.stats==null` → `s.isLoading || s.stats==null`（命中 5 分钟缓存的瞬间切换不置 isLoading，不会误闪骨架）。骨架转换块高度 168→272 镜像更高的大环卡。
- `flutter analyze` 2 文件 No issues found；纯 UI，业务逻辑零改动。
- **待用户真机验证后 commit**（连同前两轮个人统计整改一并落 PROGRESS_PERSONAL_STATS_UI 文档）。

## 个人统计页 UI 微调（第四轮，改码未提交，待真机验证）
- 用户反馈：上轮上下居中布局「环状图又和数字重叠、且要左右分区不是上下」，要求回归最初「左环右数据」。
- 修复（`conversion_ring.dart` 重写）：改回**左右分区布局**——左侧大环（环心仅放「转化率」小标签 13/secondary，绝不放大字，根除与右侧重叠），右侧 `Expanded` 放百分比大字(30/bold/primary) + 转化/线索说明(14/secondary)。
- 环放大：环宽 `constraints.maxWidth * 0.58` clamp(160, 240)，封顶 240（约为初版 120 的双倍），`strokeWidth` 18；左右布局下环受右侧数据挤压，0.58 占比接近物理上限，右侧 `Expanded` 自动换行不溢出。
- `flutter analyze` → No issues found；纯 UI，业务逻辑零改动。
- **待用户真机验证后 commit**（连同前三轮个人统计整改一并落 PROGRESS_PERSONAL_STATS_UI 文档）。

## 个人统计页 UI 微调（第五轮：删环改饼图，改码未提交，待真机验证）
- 用户反馈：上轮环形「还是原来大小、但占据空间特别大」，要求删掉环状图、重写饼图。
- 实现：`conversion_ring.dart` → `conversion_pie.dart`，类 `ConversionRing`→`ConversionPie`，用 fl_chart `PieChart` 实心饼图（centerSpaceRadius:0）。
  - 数据：两扇区「已转化=stats.converted(primary) / 未转化=leadsTotal-converted(line)」，直接取 model 原始数值（非拆字符串）。
  - 左右分区：左 `SizedBox(150×150)` 固定饼图（根除空间失控），右 `Expanded` 放百分比大字(30/bold/primary)+两行图例(色块+已转化/未转化+数值)。
  - 兜底：leadsTotal==0 时显示灰色实心圆 + "暂无"，避免 fl_chart 0 值扇区崩溃。
  - 沿用团队统计 status_donut 的 fl_chart 0.70 API（duration/curve 直传，非 swapAnimationDuration）。
- 页面同步：import 路径 + `ConversionRing`→`ConversionPie`；骨架转换块高度 272→150 镜像饼图卡。
- `flutter analyze` 2 文件 No issues found；纯 UI，业务逻辑零改动。
- 后续微调：饼图起始角 `startDegreeOffset: -90`，从圆心向上(12点)起笔（原默认0=向右），用户要求逆时针转90°。
- **待用户真机验证后 commit**（连同前四轮个人统计整改一并落 PROGRESS_PERSONAL_STATS_UI 文档）。
